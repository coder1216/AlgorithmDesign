/**
 keywords: all possible solution

 template



 returnType dfs(curr){
     // need helper???

     // base case: search success
     // base case: seach fail

     for(each_branch){
         // set value/state
         dfs(next);
         // set back value / state(in pair!!!)
     }
 }

 是否加visited?
 if yes: set visited = true; and set it back! Always come int Pair!!!!
 */