/**
 * implement stack by Queue
 */

class Solution{
    private void myQueue(){
        private void myQueue(){
            private ListNode head;
            private ListNode tail;
            int length;
        }
        public void myQueue(){
            head = new ListNode(0);
            tail = new ListNode(0);
        }
        public void offer(int val){
            if(taill == null) return null;

        }
        public int poll(){
            if(head == null) throw new IllegalArgumentException();
            ListNode<Integer> dummy = head;

        }
        public int peek(){
            
        }
    }
} 